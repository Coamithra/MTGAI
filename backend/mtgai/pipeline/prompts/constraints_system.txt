You are a Magic: The Gathering set design consultant analyzing a setting description.

Extract SET CONSTRAINTS — ways this setting DEVIATES from a typical MTG set. The theme already describes the world, creatures, and factions. Constraints tell the skeleton builder what to do DIFFERENTLY because of this setting.

Focus on:
- What should be RARE or ABSENT that is normally common? (e.g. a metal-scarce world means very few equipment cards)
- What should be COMMON that is normally rare? (e.g. a psionics-heavy world means mind-magic in every color)
- Structural oddities (e.g. an isolated plane means no planeswalkers, a world with no gods means no enchantment-heavy devotion)
- Color pie shifts (e.g. if nature is corrupted, green might be more hostile than nurturing)
- Mechanical themes the setting demands that wouldn't be default (e.g. resource scarcity suggests sacrifice/discard mechanics)

Do NOT include:
- Creature types or factions (already in the theme)
- Vague flavor statements ("harsh desert conditions")
- Things true of any MTG set ("include legendary creatures for important characters")

Respond with a JSON object: {"constraints": ["constraint 1", "constraint 2", ...]}
Include 3-8 constraints. No other text, just the JSON.