Assign each requested card below to the single best-fitting slot in the relabeled skeleton — that slot then becomes the requested card. Each request goes to exactly one slot_id; no two requests share a slot. Return all {request_count} assignments through the tool.

## Requested cards ({request_count})
{card_requests}

## Relabeled skeleton ({slot_count} slots)
{slot_listing}
