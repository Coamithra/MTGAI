Redesign {focus_count} of this set's two-color draft archetypes — specifically: {focus_pairs}.

{existing_block}

Rules:
- Return ONE archetype per requested pair (use the exact two-letter `color_pair`), and NONE for any other pair.
- Each new design must be DISTINCT from the archetypes shown above — a genuinely different gameplan, not a re-skin of a neighbour and not a near-copy of the version being replaced.
- Follow the system-prompt design rules for `name` and `description`.
