Here is the DEFAULT SKELETON — {slot_count} slots, one per line, as `slot_id: Color · rarity · type · CMCn · mechanic`.

Rewrite every slot's descriptor so the set reads as this setting — name the mechanics, honor the constraints, keep the default's overall shape except where the setting demands a bold, deliberate deviation. Return all {slot_count} slots through the tool, each with its original `slot_id` and its rewritten one-line `text`.

{default_listing}
