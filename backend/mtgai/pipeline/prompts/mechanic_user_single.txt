Design ONE new mechanic candidate for this set. We generate candidates one at a time so each design stays sharp; you only need to produce a single, complete mechanic now.

This is candidate #{position} of {target} we are collecting; we will ultimately select {mechanic_count} of them for the set.

{already_block}

Requirements for THIS mechanic:
- It MUST be DISTINCT from every mechanic already designed above — a genuinely different mechanical space, not a re-skin or minor variation.
- DELIBERATELY VARY from the ones above: pick a different color identity and a different complexity than the cluster already designed (the set needs a spread, not near-identical mechanics). If they are all simple, design a richer complexity-2 or complexity-3 one; if they all share colors, branch into the under-used colors. Varying the SHAPE helps too — if the set so far is all triggered abilities, try an activated cost, a static keyword, or a keyword action.
- Must fit the set's theme and flavor (see system prompt) and support 2+ of the set's draft archetypes.

Pin down the definition before you write the examples:
1. Choose the SHAPE first. Decide which shape from the system prompt your idea is (bare static A, numeric `Keyword N` B, activated-cost C, triggered D, static/replacement E, casting-modifier F, the Kicker shape G, an ability word, or a keyword action), then write `reminder_text` in that shape's exact form. Set `keyword_type` to that shape's bucket. If the effect's size naturally varies across cards, make it shape B (`Keyword N`) rather than fixing one value.
2. Make the reminder self-contained. A player resolves the mechanic from the reminder alone — define every counter, token, or sub-state it invokes. The ONE exception is the Kicker shape (G): there the reminder states only the shared cost/trigger and each card writes its own effect (never an [effect] placeholder).

Provide EXACTLY TWO example cards (`example_cards`) that use this mechanic, at contrasting rarities — typically one common showing the simple version and one rare/mythic showing a richer expression — so downstream card generation has concrete reference designs across the rarity range. Each example must have complete, valid MTG templating (mana cost in braces, full type line, oracle text using the mechanic keyword). For every shape except G, BOTH examples do exactly what the reminder says (a richer rare is a bigger N, not a different effect); for the Kicker shape (G) the two examples carry different card-defined effects in their text boxes. Do NOT write parenthesized reminder text in an example's oracle text — it is injected automatically later.
{needed_hint}{regen_block}

Before you submit, check:
1. Which shape is this, and does the reminder match that shape's form exactly?
2. Is it DISTINCT from the mechanics above in space, color, AND complexity?
3. Does the reminder resolve unambiguously on its own (or, for the Kicker shape, define the shared cost/trigger with the effect on the card)?
4. Does it have design space for {expected_mechanic_density} cards in a {set_size}-card set, across common, uncommon, and rare?
5. Do both example cards use the keyword by name with correct templating, and show a card-generation model how to write this mechanic on a real card?

Return exactly one mechanic in the tool call.
