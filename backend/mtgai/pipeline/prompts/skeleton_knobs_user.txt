Tune the structural knobs for this {set_size}-card set to fit the theme, mechanics,
archetypes, constraints, and knob reference provided above.

Move a knob only when the theme calls for it; otherwise leave it at its default.
Propose cycles only when the theme wants a coherent multi-card family.

Return tuned values via the submit_skeleton_knobs tool.
