# MTGAI Model Registry
#
# Defines all available LLM and image-generation models.
# Edit this file to add new models or providers.
# Keys (e.g. [llm.opus]) are short identifiers used in settings profiles.
#
# Local models use the llamacpp provider in managed mode: llmfacade owns a
# llama-swap subprocess that supervises llama-server instances on demand.
# Each local model needs:
#   - gguf_path: absolute path to the .gguf file on disk
#   - context_window: --ctx-size at server launch (also used for budget calcs)
# Optional launch knobs (per-model KV-cache quant, GPU offload):
#   - cache_type_k / cache_type_v: "f16" (default), "q8_0", "q4_0"
#   - n_gpu_layers: number of layers to offload to GPU (-1 / 999 = all)

# ── LLM Models ──────────────────────────────────────────────────────────────

[llm.opus]
name = "Claude Opus 4.6"
provider = "anthropic"
model_id = "claude-opus-4-6"
tier = 3
input_price = 15.00   # per 1M tokens
output_price = 75.00  # per 1M tokens
context_window = 200000
supports_effort = true
supports_vision = true
supports_caching = true

[llm.sonnet]
name = "Claude Sonnet 4.6"
provider = "anthropic"
model_id = "claude-sonnet-4-6"
tier = 2
input_price = 3.00
output_price = 15.00
context_window = 200000
supports_effort = false
supports_vision = true
supports_caching = true

[llm.haiku]
name = "Claude Haiku 4.5"
provider = "anthropic"
model_id = "claude-haiku-4-5-20251001"
tier = 1
input_price = 0.80
output_price = 4.00
context_window = 200000
supports_effort = false
supports_vision = true
supports_caching = true

[llm.qwen-14b]
name = "Qwen 2.5 14B (Local)"
provider = "llamacpp"
model_id = "qwen2.5-14b"
gguf_path = "C:/Models/qwen2.5-14b.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 32768
supports_effort = false
supports_vision = false
supports_caching = false
n_gpu_layers = -1

[llm.qwen-3b]
name = "Qwen 2.5 3B (Local)"
provider = "llamacpp"
model_id = "qwen2.5-3b"
gguf_path = "C:/Models/qwen2.5-3b.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 32768
supports_effort = false
supports_vision = false
supports_caching = false
n_gpu_layers = -1

[llm.qwen3-4b]
name = "Qwen 3.5 4B (Local)"
provider = "llamacpp"
model_id = "qwen3.5-4b"
gguf_path = "C:/Models/qwen3.5-4b.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 32768
supports_effort = false
supports_vision = false
supports_caching = false
n_gpu_layers = -1

[llm.phi4-mini]
name = "Phi-4 Mini (Local)"
provider = "llamacpp"
model_id = "phi4-mini"
gguf_path = "C:/Models/phi4-mini.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 32768
supports_effort = false
supports_vision = false
supports_caching = false
n_gpu_layers = -1

[llm.llama3-3b]
name = "Llama 3.2 3B (Local)"
provider = "llamacpp"
model_id = "llama3.2-3b"
gguf_path = "C:/Models/llama3.2-3b.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 32768
supports_effort = false
supports_vision = false
supports_caching = false
n_gpu_layers = -1

# Best long-context config on 12 GB VRAM (per TC-1f). Vlad's IQ4_XS quant +
# q8_0 KV cache + flash attention runs a 58K-token extraction in ~4 min at
# 128K context. Per-model `cache_type_k`/`cache_type_v` here replace the
# global OLLAMA_KV_CACHE_TYPE env var that Ollama forced — llama.cpp gives
# us per-server KV quant via `--cache-type-k`/`-v`. Flash attention is
# enabled by default in llama-server when the architecture supports it.
[llm.gemma4-26b-vram-dynamic]
name = "Gemma 4 26B Vlad IQ4_XS (Local)"
provider = "llamacpp"
model_id = "vlad-gemma4-26b-dynamic"
gguf_path = "C:/Models/vlad-gemma4-26b-dynamic.gguf"
tier = 0
input_price = 0.00
output_price = 0.00
context_window = 128000
supports_effort = false
# Gemma 4 has a vision projector, but managed-mode llamacpp here doesn't yet
# pass --mmproj at server launch. Flip to true once mmproj_path is plumbed
# through the registry + _llamacpp_new_model.
supports_vision = false
supports_caching = false
n_gpu_layers = -1
cache_type_k = "q8_0"
cache_type_v = "q8_0"

# ── Image Generation Models ─────────────────────────────────────────────────

[image.flux-local]
name = "Flux.1-dev (Local ComfyUI)"
provider = "comfyui"
cost_per_image = 0.00
implemented = true

[image.gemini-imagen]
name = "Gemini Imagen"
provider = "gemini"
cost_per_image = 0.00
implemented = false

[image.openai-dalle]
name = "OpenAI DALL-E 3"
provider = "openai"
cost_per_image = 0.04
implemented = false
